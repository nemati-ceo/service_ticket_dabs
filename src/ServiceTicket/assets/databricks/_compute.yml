# =============================================================================
# DRAFT — replace with your real ServiceTicket _compute.yml.
# ServiceTicket runs on an EXISTING cluster (var.EXISTING_CLUSTER_ID), so no
# new_cluster block is required here. To use ephemeral job clusters instead,
# define job_clusters below and reference job_cluster_key in _workflow.yml.
# =============================================================================
#
# resources:
#   jobs:
#     service_ticket_pipeline:
#       job_clusters:
#         - job_cluster_key: pipeline_cluster
#           new_cluster:
#             spark_version: 15.4.x-scala2.12
#             node_type_id: m5d.large
#             autoscale:
#               min_workers: 1
#               max_workers: 8
